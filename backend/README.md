# Backend API

## Variables de entorno
Configure las variables en el archivo .env usando las credenciales de Supabase.

## Migraciones
- npm run prisma:generate
- npm run prisma:migrate
- npm run prisma:seed
